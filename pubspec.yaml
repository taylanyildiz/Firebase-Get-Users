name: firebase_sign_api
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2

  font_awesome_flutter:

  provider:

  firebase_auth:
  firebase_core:

  google_sign_in:
  #flutter_facebook_auth:

  cloud_firestore:

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:


  uses-material-design: true
